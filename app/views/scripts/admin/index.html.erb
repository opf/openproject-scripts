<% html_title t(:label_administration), t("scripts.plural") %>

<%=
  render(Primer::OpenProject::PageHeader.new) do |header|
    header.with_title { t("scripts.plural") }
    header.with_breadcrumbs(
      [{ href: admin_index_path, text: t("label_administration") },
       { href: admin_settings_api_path, text: t("menus.admin.api_and_webhooks") },
       t("scripts.plural")]
    )
  end
%>

<%=
  render(Primer::OpenProject::SubHeader.new) do |subheader|
    subheader.with_action_button(
      scheme: :primary,
      leading_icon: :plus,
      label: I18n.t("scripts.label_add_new"),
      tag: :a,
      href: new_admin_script_path
    ) do
      t("scripts.singular")
    end
  end
%>

<%= render EnterpriseEdition::BannerComponent.new(:running_scripts, variant: :inline) %>

<%=
  render(OpenProject::Common::BorderBoxListComponent.new(container: "op-admin-scripts--list", mb: 4)) do |list|
    list.with_header(title: t("scripts.plural"))

    @scripts.each do |script|
      list.with_item { render(Scripts::Admin::RowComponent.new(script: script)) }
    end

    list.with_empty_state(
      title: t("scripts.no_results_table"),
      test_selector: "op-admin-scripts--placeholder"
    )
  end
%>
